<%= form_with model: report, url: reports_path, class: "flex flex-col gap-4 sm:flex-row sm:items-end sm:gap-3 sm:justify-between" do |f| %>
  <div class="flex sm:flex-row flex-col sm:items-end gap-4 sm:gap-3">
    <div class="w-full sm:w-64">
      <%= f.label :user_id, "User", class: "block text-xs font-medium text-slate-600 mb-1.5" %>
      <%= f.collection_select :user_id, users, :id, :name,
        { prompt: "Select user" },
        class: form_select_input_classes %>
    </div>
    <div class="w-full sm:w-64">
      <%= f.label :report_type, "Report type", class: "block text-xs font-medium text-slate-600 mb-1.5" %>
      <%= f.select :report_type,
        Report.report_types.keys.map { |t| [t.titleize, t] },
        { prompt: "Select type" },
        class: form_select_input_classes %>
    </div>
  </div>
  <%= f.button type: "submit",
    class: "shrink-0 inline-flex items-center justify-start gap-2 rounded-lg bg-slate-900 px-8 py-3 min-w-[10rem] text-base font-medium text-white shadow-sm hover:bg-slate-800 focus:outline-none focus:ring-2 focus:ring-slate-500 focus:ring-offset-2 transition-colors cursor-pointer" do %>
    <%= icon "document-plus", library: "heroicons", variant: "outline", stroke_width: 2, class: "h-5 w-5 shrink-0" %>
    Generate
  <% end %>
<% end %>
