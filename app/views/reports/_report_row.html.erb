<tr id="<%= dom_id(report) %>" class="group hover:bg-slate-50/80 transition-colors <%= 'animate-fade-out-background' if report.previously_new_record? || report.processing? %>">
  <td class="px-6 py-4 text-slate-600 text-sm"><%= report_type_label(report) %></td>
  <td class="px-6 py-4">
    <%= status_badge_html(report) %>
  </td>
  <td class="px-6 py-4 text-slate-600 text-sm"><%= report.user.name %></td>
  <td class="px-6 py-4 text-slate-500 text-sm"><%= report_formatted_created_at(report) %></td>
  <td class="px-6 py-4 text-right">
    <% if report.downloadable? %>
      <%= link_to download_report_path(report),
          class: "inline-flex items-center gap-1.5 px-3 py-1.5 bg-white border border-slate-200 text-slate-700 text-sm font-medium rounded-lg hover:bg-slate-50 hover:text-slate-900 focus:outline-none focus:ring-2 focus:ring-slate-200 transition-colors shadow-sm no-underline",
          data: { turbo_frame: "_top" } do %>
        <%= icon "arrow-down-tray", library: "heroicons", stroke_width: 2, class: "h-4 w-4 shrink-0" %>
        Download
      <% end %>
    <% else %>
      <span class="text-slate-400 text-sm italic">Not available</span>
    <% end %>
  </td>
</tr>
