<% cell_classes = "px-4 py-3 sm:px-6 sm:py-4" %>
<% animation_class = 'animate-fade-out-background' if report.previously_new_record? || report.processing? %>

<div id="<%= dom_id(report) %>" class="report-item min-h-18 grid grid-cols-1 md:grid-cols-5 md:gap-4 group hover:bg-slate-50/80 transition-colors border-b border-slate-100 last:border-b-0 <%= animation_class %>" role="row">
  <div class="<%= cell_classes %> md:flex md:items-center" role="gridcell">
    <p class="md:hidden font-medium text-slate-500 text-xs uppercase tracking-wider">Report type</p>
    <p class="mt-0.5 md:mt-0 md:font-normal font-medium text-slate-600 text-sm"><%= report_type_label(report) %></p>
  </div>
  <div class="<%= cell_classes %> md:flex md:items-center" role="gridcell">
    <p class="md:hidden font-medium text-slate-500 text-xs uppercase tracking-wider">Status</p>
    <div class="mt-0.5 md:mt-0"><%= status_badge_html(report) %></div>
  </div>
  <div class="<%= cell_classes %> md:flex md:items-center" role="gridcell">
    <p class="md:hidden font-medium text-slate-500 text-xs uppercase tracking-wider">User</p>
    <p class="mt-0.5 md:mt-0 text-slate-600 text-sm"><%= report.user.name %></p>
  </div>
  <div class="<%= cell_classes %> md:flex md:items-center" role="gridcell">
    <p class="md:hidden font-medium text-slate-500 text-xs uppercase tracking-wider">Created</p>
    <p class="mt-0.5 md:mt-0 text-slate-500 text-sm"><%= report_formatted_created_at(report) %></p>
  </div>
  <div class="<%= cell_classes %> md:flex md:items-center md:justify-end pt-2" role="gridcell">
    <%= render "reports/report_download_action", report: report %>
  </div>
</div>
