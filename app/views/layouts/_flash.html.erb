<div id="flash">
  <% flash.each do |type, message| %>
    <% if message.present? %>
      <div class="<%= flash_class(type) %>" role="alert">
        <%= message %>
      </div>
    <% end %>
  <% end %>
</div>
