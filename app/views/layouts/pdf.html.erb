<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= content_for(:title) || "Report" %></title>
    <style>
      body { font-family: system-ui, sans-serif; font-size: 12px; line-height: 1.5; color: #333; margin: 0; padding: 20px; }
      h1 { font-size: 18px; margin: 0 0 16px 0; border-bottom: 2px solid #111; padding-bottom: 8px; }
      h2 { font-size: 14px; margin: 16px 0 8px 0; }
      table { width: 100%; border-collapse: collapse; margin-bottom: 16px; }
      th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
      th { background: #f5f5f5; font-weight: 600; }
      .meta { color: #666; font-size: 11px; margin-bottom: 16px; }
      .amount, .quantity { text-align: right; padding-left: 0.5em; }
      .status { white-space: nowrap; }
      .inline-amount { white-space: nowrap; }
      .inline-amount .formatted-numeric { display: inline-flex; width: auto; align-items: baseline; gap: 0.25em; }
      .formatted-numeric { display: table; width: 100%; }
      .numeric-symbol { display: table-cell; width: 2em; padding-right: 0.35em; font-weight: 500; vertical-align: baseline; }
      .numeric-value { display: table-cell; text-align: right; font-family: ui-monospace, "SF Mono", "Cascadia Code", Consolas, Monaco, monospace; font-variant-numeric: tabular-nums; }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
